# Copy to .env and fill in. Never commit .env.
SMS_GATE_SERVER=https://api.sms-gate.app
SMS_GATE_USERNAME=
SMS_GATE_PASSWORD=
# Optional: minutes; status.sh treats lastSeen older than this as stale (default 20)
SMS_GATE_ACTIVE_WITHIN_MINUTES=20
